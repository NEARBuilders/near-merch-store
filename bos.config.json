{
  "account": "near-merch-store.near",
  "app": {
    "host": {
      "title": "Near Merch",
      "development": "http://localhost:3001",
      "production": "https://near.everything.market"
    },
    "ui": {
      "name": "marketplace_ui",
      "development": "http://localhost:3002",
      "production": "https://elliot-braem-1144-marketplace-ui-near-merch-store-6f46d060b-ze.zephyrcloud.app",
      "exposes": {
        "App": "./App",
        "components": "./components",
        "providers": "./providers",
        "types": "./types"
      }
    },
    "api": {
      "plugins": {
        "marketplace-api": {
          "development": "http://localhost:3014",
          "production": "https://elliot-braem-1143-marketplace-api-near-merch-stor-c0aea0c3a-ze.zephyrcloud.app",
          "variables": {
            "network": "mainnet",
            "contractId": "social.near"
          },
          "secrets": {
            "RELAYER_ACCOUNT_ID": "{{RELAYER_ACCOUNT_ID}}",
            "RELAYER_PRIVATE_KEY": "{{RELAYER_PRIVATE_KEY}}",
            "STRIPE_SECRET_KEY": "{{STRIPE_SECRET_KEY}}",
            "STRIPE_WEBHOOK_SECRET": "{{STRIPE_WEBHOOK_SECRET}}",
            "PRINTFUL_API_KEY": "{{PRINTFUL_API_KEY}}",
            "DATABASE_URL": "{{DATABASE_URL}}",
            "DATABASE_AUTH_TOKEN": "{{DATABASE_AUTH_TOKEN}}"
          }
        }
      }
    }
  }
}
